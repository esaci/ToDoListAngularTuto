import { TacheStatusDirective } from './tache-status.directive';

describe('TacheStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new TacheStatusDirective();
    expect(directive).toBeTruthy();
  });
});
