import { TacheDisplayDirective } from './tache-display.directive';

describe('TacheDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new TacheDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
